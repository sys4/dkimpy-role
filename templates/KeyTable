# {{ ansible_managed }}
# identifier senderdomain:selector:/path/to/signing.key

{% for key, value in rsa_keys.items() %}
{% if value.enabled == True %}
{{ key }}    {{ value.sender | regex_replace('^.*@(.*)$','\\1') }}:{{ value.selector }}:{{ dkimpy_key_store }}/{{ value.selector }}.rsa.{{ value.sender }}.key
{% endif %}
{% endfor %}

# vim: set ft=jinja:
